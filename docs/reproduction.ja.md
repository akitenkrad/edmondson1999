[English](reproduction.md) | **日本語**

# 再現

本モデルは，シミュレートされたチーム横断データを原著が 51 チーム標本を扱うのと同じ方法で扱うことで，Edmondson (1999) のフィールド調査の数値に対応づける．すなわちチームレベル構成概念上で ICC と Baron & Kenny (1986) 三段階媒介分析を計算する．

## 安定なチームレベル測度

チーム学習 `L_k` とパフォーマンス `Π_k` は小規模 (n_k≈8) チームでは単一 tick のノイジーな率である．再現では各チームの **実行後半の時間平均** を安定な survey 相当の測度として用いる — これがまさに `team_cross_section.csv` に書かれる横断データである．ICC(ψ) は最終 tick の個人別 ψ で計算し，ICC(L) は各個人の時間平均発言率をチームでグループ化して計算する．

## 試行ごとの平均

`reproduce` は `--runs` 回の独立試行を実行し，**各** 試行の約 90 チームの横断データでアンカーを計算してから統計量を平均する．これにより原著の n≈51 の検定力を保つ．全試行を 1 つの回帰にプールすると検定力が過大になり，非有意であるべき媒介後残差まで「有意」になってしまう．

## 較正アンカー (§5)

| 指標 | 論文値 | 目標帯 |
|------|--------|--------|
| ICC(ψ) | .39 | [.25, .55] |
| ICC(L) | .27 | [.15, .40] |
| ψ→L 傾き B (Table 5A) | .76 | [.50, 1.00] |
| ψ→L 調整 R² (Table 5A) | .63 | [.48, .78] |
| L→Π 調整 R² (Table 4) | .26 | [.16, .36] |
| support→ψ 傾き B (Table 6) | .56 | [.35, .77] |
| 媒介後 ψ 残差 | B=.25, p=.42 | p > .10 (非有意) |
| 媒介比率 (a·b/c) | — | ≥ .5 |
| ψ 統制下の効力感偏 |t| (H5/H8) | < 2 | < 2 |
| 支持仮説数 | 6/8 | ≥ 5/8 |

## Rust と Python

- `cargo run --release -- reproduce` は `metrics.rs` のローカル OLS / ICC / Baron & Kenny 実装でアンカーを表示する．
- `uv run edmondson-tools reproduce` は `team_cross_section.csv` を読み，**statsmodels** で三段階媒介分析を試行ごとに再計算し (正確 t の p 値)，間接効果 a·b の **ブートストラップ** (95% バイアス補正パーセンタイル CI) を加え，効力感弁別 `L ~ ψ + efficacy` を実行し，`table4_report.csv` + `mediation_bootstrap.csv` を書く．2 経路はアンカー判定で一致する．

## H5 / H8 (効力感弁別)

チーム効力感 η_k はチーム別の **独立** 潜在目標へ進化する (ψ でも performance でもない)．ψ を統制すると効力感は学習行動についての情報をほとんど持たず，偏 |t| は 2 を下回る — 心理的安全性を一定にすると効力感が学習を説明し **ない** という原著所見 (H5・H8 不支持) を再現する．したがって正しい再現は H5/H8 を支持仮説の集計から外し，8 中 6 に着地する．

---
*This file was generated by Claude Code.*
